# Database
DATABASE_URL="postgresql://username:password@localhost:5432/smart_pos"

# JWT Secret
JWT_SECRET="your-secret-key-here-change-in-production"

# Email Configuration (for low stock alerts and order notifications)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-specific-password"
SMTP_FROM_NAME="Smart POS"

# Frontend URL (for email links)
FRONTEND_URL="http://localhost:5173"

# Manager Email (for low stock alerts)
MANAGER_EMAIL="manager@yourstore.com"
